# - Manages distributed datasets.
# - addDataset, getDataset, removeDataset, existsDataset: Methods for managing datasets identified by UUIDs.

# DSI -> Query(take graph X with UUID Y and return it)
# DSII -> hsdf://<hdfs_host>:<hdfs_port>/path/to/graphX