participantgroup #lightblue **metaphactory**
participant metaphactory
end

participantgroup #orange **Data Sources**
database Graph Database
end

participantgroup #lightgrey **Graph-Massivizer Toolkit**
database BGO Repository
participantgroup #darkgrey Graph-Massivizer Tools
participant Graph-Inceptor
participant Graph-Scrutinizer
participant Graph-Optimizer
end
end

alt #white case 1
Graph-Optimizer->Graph Database:get_graphs_for_graph_handle
else case 2
Graph-Inceptor->Graph Database:get_graphs_for_graph_handle
Graph-Inceptor->Graph-Optimizer:result_graphs_for_graph_handle_event
end

alt #white case 1
Graph-Optimizer->BGO Repository:get_bgo_implementations
else case 2
Graph-Scrutinizer->BGO Repository :get_bgo_implementations
Graph-Scrutinizer->Graph-Optimizer :result_bgo_implementations_event
end

Graph-Optimizer->Graph-Optimizer :bgo_execution_cost_estimates