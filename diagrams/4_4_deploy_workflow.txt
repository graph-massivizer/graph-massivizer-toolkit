participantgroup #lightblue **metaphactory**
participant metaphactory
end

participantgroup #lightgrey **Graph-Massivizer Toolkit**
database BGO Repository
participant Graph-Choreographer
end

participantgroup #pink **Infrastructure**
participant Compute Nodes #violet
end

group #lightgrey Deploy BGO workflow
loop For each BGO in the workflow
ref over metaphactory, Graph-Choreographer: Lookup BGO implementations
Graph-Choreographer->Graph-Choreographer:Prepare BGO deployment
Graph-Choreographer->Graph-Choreographer:Deploy BGO implementation

ref over metaphactory, Compute Nodes: Execute BGO workflow

end

end
