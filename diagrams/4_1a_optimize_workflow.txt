participantgroup #lightblue **metaphactory**
participant metaphactory
end

participantgroup #orange **Data Sources**
database Graph Database
end

participantgroup #lightgrey **Graph-Massivizer Toolkit**
database BGO Repository
participantgroup #darkgrey Graph-Massivizer Tools
participant Graph-Choreographer
participant Graph-Optimizer
end
end

metaphactory-#blue:2>Graph-Choreographer:Request BGO workflow execution

group #lightgrey Optimise BGO workflow
Graph-Choreographer->Graph-Optimizer:Request BGO execution cost

ref over metaphactory, Graph-Optimizer: Lookup graphs for graphs handle

ref over metaphactory, Graph-Optimizer: Lookup BGO implementations
Graph-Optimizer->Graph-Optimizer :Estimate BGO execution cost
Graph-Optimizer-#red:2>Graph-Choreographer :Report BGO workflow execution cost
end
