participantgroup #lightblue **metaphactory**
participant metaphactory
end

participantgroup #orange **Data Sources**
database Graph Database
end

participantgroup #lightgrey **Graph-Massivizer Toolkit**
database BGO Repository
participantgroup #darkgrey Graph-Massivizer Tools
participant Graph-Choreographer
participant Graph-Optimizer
end
end

metaphactory-#blue:2>Graph-Choreographer:workflow_execution_request

group #lightgrey Graph-Optimizer
Graph-Choreographer->Graph-Optimizer:bgo_execution_cost_estimates_request

ref over metaphactory, Graph-Optimizer: (4.1.1) Get graphs for graphs handle

ref over metaphactory, Graph-Optimizer: (4.1.2) Get BGO implementations
Graph-Optimizer->Graph-Optimizer :calculate_bgo_execution_cost_estimates
Graph-Optimizer-#red:2>Graph-Choreographer :bgo_execution_cost_estimates
end