participantgroup #lightblue **metaphactory**
participant metaphactory
end

//participantgroup #orange **Data Sources**
//database Graph Database
//end

participantgroup #lightgrey **Graph-Massivizer Toolkit**
database BGO Repository
participantgroup #darkgrey Graph-Massivizer Tools
participant Graph-Greenifier
participant Graph-Choreographer
end
end

participantgroup #pink **Infrastructure**
participant Compute Nodes #violet
end

group #lightgrey Schedule BGO workflow
Graph-Choreographer<#blue:2-Graph-Greenifier:Propose green execution plans
Graph-Choreographer->Graph-Choreographer:Select green execution plan
Graph-Choreographer->Graph-Choreographer:Schedule BGO workflow
ref over metaphactory, Compute Nodes: Deploy BGO workflow
ref over metaphactory, Compute Nodes: Execute BGO workflow

end
